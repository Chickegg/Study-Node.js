현재 Web의 홈으로 오게되면 undefined 또는 null로 나올 것이다.
이것은 id값을 우리가 html코드에 넣어주기 때문이다.

그렇다면 id값이 null, undefined라면❓ 조건문의 중첩을 이용해서
if(pathname === '/') 이고
    if(id === 'null') 이라면? {
        let id = 'Welcome'으로 바꿔주고
        description = 'Hello, Node.js'로 변경 // let은 변경은 가능하지만 재선언은 안된다.
        let template = `html code, {id},{description} 등등으로 이루어져 있다라고 선언

        response.writeHead(200); // 제대로 되었다.
        response.end(template) // 변수에 저장해둔 html code를 화면에 띄어준다.
    }
이런식으로 진행해 보았다.
